# E2E Test Router

Stage: `e2e_test`

Primary executor: qa-e2e.

The active stage is selected from `.ai-dev-template.workflow-state.json`.

Use this branch only while `current_stage = "e2e_test"`.

Read next:

1. `instructions/e2e-test/qa-e2e.md`
